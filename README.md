# React To-Do List
An app to track your todo list

# Installation and Run Instructions
1. Clone the repo
2. cd into repo directory 'todo-list'
3. Run command ```npm install``` to install the required packages
4. Run command ```npm run dev``` to run the development server
5. Go to ```http://localhost:5173/``` in your browser